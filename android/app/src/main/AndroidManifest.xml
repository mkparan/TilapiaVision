<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Camera capture — Section 3.8.2 Input Layer -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <!-- No INTERNET permission — deliberately omitted. TilapiaVision
         runs fully offline (Section 3.8, "No cloud connection"); every
         permission requested here should map to something the app
         genuinely does on-device. -->

    <!-- NOTE: no android:icon set — no launcher icon PNGs are included
         in this rebuild (I don't have your brand assets to generate
         them from). Android falls back to a default icon without one;
         run `flutter create .` over this project once to regenerate
         the standard mipmap files, or supply your own via
         android/app/src/main/res/mipmap-*/ic_launcher.png. -->
    <application
        android:label="TilapiaVision"
        android:name="${applicationName}">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>
